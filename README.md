# Online Poll App

Приложение для проведения онлайн-опросов с поддержкой различных типов вопросов.

## 🚀 Функциональность

- **Многостраничные опросы**: поддержка опросов с несколькими страницами вопросов
- **Типы вопросов**:
  - Single choice (выбор одного варианта)
  - Multiple choice (выбор нескольких вариантов)
- **Валидация**: обязательные и необязательные вопросы
- Опциональная кнопка "Затрудняюсь ответить" для каждого вопроса
- **Прогресс-бар**: визуальный индикатор прогресса прохождения опроса

## 🛠️ Стек технологий

- **React 19**
- **Tailwind CSS 4** 
- **shadcn/ui** 
- **MobX**
- **React Router v6**
- **TypeScript**
- **Vite**

## 📦 Установка и запуск

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

## 🏗️ Архитектура

- `app/` - конфигурация приложения, роутинг, layout
- `pages/` - страницы приложения
- `widgets/` - крупные UI блоки
- `features/` - бизнес-функциональность (вопросы, навигация)
- `entities/` - бизнес-сущности (опросы, ответы)
- `shared/` - переиспользуемые компоненты, утилиты, типы

## 📋 Демо-данные

Приложение включает демо-конфигурацию опроса разработчиков с различными типами вопросов.

## 🔧 Доступные команды

```bash
npm run dev      # Запуск в режиме разработки
npm run build    # Сборка для продакшена
npm run lint     # Проверка кода линтером
npm run preview  # Предпросмотр собранного приложения
```

## 📄 Лицензия

MIT